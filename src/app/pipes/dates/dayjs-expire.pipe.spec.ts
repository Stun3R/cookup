import { DayjsExpirePipe } from './dayjs-expire.pipe';

describe('DayjsExpirePipe', () => {
  it('create an instance', () => {
    const pipe = new DayjsExpirePipe();
    expect(pipe).toBeTruthy();
  });
});
